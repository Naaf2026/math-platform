"use client";

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { ArrowLeft, ArrowRight, CheckCircle2, Flame, Home, RotateCcw, Sparkles, Target, Trophy, XCircle, Zap } from "lucide-react";
import { createClient } from "@/lib/supabase/client";

type Question={id:string;topicId:string;topic:string;prompt:string;options:string[];answer:string;explanation:string;difficulty:string;skill:string;points:number};
type Profile={xp:number;current_streak:number;best_streak:number};

function levelForXp(xp:number){return Math.floor(Math.max(0,xp)/100)+1;}
function levelStart(level:number){return (level-1)*100;}

export default function MissionPage(){
 const[questions,setQuestions]=useState<Question[]>([]),[loading,setLoading]=useState(true),[current,setCurrent]=useState(0),[selected,setSelected]=useState<string|null>(null),[score,setScore]=useState(0),[correct,setCorrect]=useState(0),[finished,setFinished]=useState(false),[saving,setSaving]=useState(false),[error,setError]=useState(""),[totalXp,setTotalXp]=useState(0),[streak,setStreak]=useState(0),[bestStreak,setBestStreak]=useState(0);
 const q=questions[current];
 const progress=questions.length?((current+(selected?1:0))/questions.length)*100:0;
 const level=useMemo(()=>levelForXp(totalXp),[totalXp]);
 const currentLevelStart=levelStart(level),nextLevelXp=level*100,levelProgress=Math.min(100,Math.round(((totalXp-currentLevelStart)/100)*100));

 useEffect(()=>{loadMission();},[]);

 async function loadMission(){
  const supabase=createClient();
  if(!supabase){setError("Your learning account is not configured yet.");setLoading(false);return;}
  const{data:{user}}=await supabase.auth.getUser();
  if(!user){window.location.href="/login";return;}
  const[{data,error},{data:profile}]=await Promise.all([
    supabase.rpc("get_adaptive_questions",{p_limit:10}),
    supabase.from("profiles").select("xp,current_streak,best_streak").eq("id",user.id).maybeSingle()
  ]);
  if(error||!data?.length){setError(error?.message||"No practice questions are available yet.");setLoading(false);return;}
  setQuestions(data.map((item:Question)=>({...item,options:Array.isArray(item.options)?item.options:[]})));
  setTotalXp(profile?.xp??0);setStreak(profile?.current_streak??0);setBestStreak(profile?.best_streak??0);setLoading(false);
 }

 async function choose(option:string){
  if(selected||!q)return;
  setSelected(option);setSaving(true);setError("");
  const supabase=createClient();
  if(!supabase){setError("Your learning account is not configured yet.");setSaving(false);return;}
  const{data,error}=await supabase.rpc("submit_learning_answer",{p_question_id:q.id,p_selected_answer:option});
  if(error){setError("Your answer could not be saved. Please try again.");setSaving(false);return;}
  const result=data?.[0];
  if(result?.is_correct){const earned=result.xp_awarded||0;setCorrect(v=>v+1);setScore(v=>v+earned);setTotalXp(v=>v+earned);}
  const{data:profile}=await supabase.from("profiles").select("xp,current_streak,best_streak").maybeSingle();
  if(profile){setTotalXp(profile.xp??0);setStreak(profile.current_streak??0);setBestStreak(profile.best_streak??0);}
  setSaving(false);
 }

 function next(){if(!selected)return;if(current===questions.length-1){setFinished(true);return;}setCurrent(v=>v+1);setSelected(null);}
 function restart(){setQuestions([]);setCurrent(0);setSelected(null);setScore(0);setCorrect(0);setFinished(false);setError("");setLoading(true);loadMission();}

 if(loading)return <main className="min-h-screen bg-gradient-to-br from-violet-50 via-white to-cyan-50 p-6"><div className="mx-auto flex min-h-[70vh] max-w-xl items-center justify-center"><div className="rounded-[2rem] bg-white p-10 text-center shadow-xl"><div className="mx-auto h-12 w-12 animate-spin rounded-full border-4 border-violet-100 border-t-violet-600"/><p className="mt-5 font-black text-[#071b3a]">Building your personalised mission…</p><p className="mt-2 text-sm text-slate-500">Choosing questions from your learning history.</p></div></div></main>;

 if(finished)return <main className="min-h-screen bg-gradient-to-br from-violet-50 via-white to-cyan-50 p-5 sm:p-8"><div className="mx-auto max-w-3xl"><header className="flex items-center justify-between"><Link href="/dashboard" className="inline-flex items-center gap-2 font-bold text-slate-600"><Home size={18}/> Dashboard</Link><span className="rounded-full bg-white px-4 py-2 text-sm font-black text-violet-600 shadow-sm">Mission complete</span></header><section className="mt-8 overflow-hidden rounded-[2rem] bg-gradient-to-br from-violet-600 via-indigo-600 to-blue-500 p-8 text-center text-white shadow-2xl sm:p-12"><div className="mx-auto flex h-20 w-20 items-center justify-center rounded-3xl bg-yellow-300 text-violet-900 shadow-xl"><Trophy size={40}/></div><p className="mt-6 text-sm font-black uppercase tracking-[0.18em] text-cyan-200">Adaptive mission finished</p><h1 className="mt-2 text-4xl font-black sm:text-5xl">Maths champion! 🎉</h1><p className="mx-auto mt-3 max-w-xl text-indigo-100">Your next mission will use what you got right and where you need more practice.</p><div className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-4"><div className="rounded-2xl bg-white/10 p-4"><Zap className="mx-auto text-yellow-300" size={22}/><p className="mt-2 text-2xl font-black">{score}</p><p className="text-xs text-indigo-100">XP earned</p></div><div className="rounded-2xl bg-white/10 p-4"><Trophy className="mx-auto text-yellow-200" size={22}/><p className="mt-2 text-2xl font-black">Level {level}</p><p className="text-xs text-indigo-100">Current level</p></div><div className="rounded-2xl bg-white/10 p-4"><Flame className="mx-auto text-orange-300" size={22}/><p className="mt-2 text-2xl font-black">{streak}</p><p className="text-xs text-indigo-100">Day streak</p></div><div className="rounded-2xl bg-white/10 p-4"><CheckCircle2 className="mx-auto text-cyan-200" size={22}/><p className="mt-2 text-2xl font-black">{correct}/{questions.length}</p><p className="text-xs text-indigo-100">Correct</p></div></div><div className="mx-auto mt-6 max-w-xl rounded-2xl bg-white/10 p-4 text-left"><div className="flex items-center justify-between text-xs font-bold"><span>Level {level} progress</span><span>{totalXp} / {nextLevelXp} XP</span></div><div className="mt-2 h-3 overflow-hidden rounded-full bg-white/15"><div className="h-full rounded-full bg-yellow-300 transition-all" style={{width:`${levelProgress}%`}}/></div><p className="mt-2 text-xs text-indigo-100">Best streak: {bestStreak} days · {Math.max(0,nextLevelXp-totalXp)} XP to Level {level+1}</p></div><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><button onClick={restart} className="inline-flex items-center justify-center gap-2 rounded-2xl bg-yellow-300 px-5 py-3.5 font-black text-violet-950 hover:bg-yellow-200"><RotateCcw size={18}/> New adaptive mission</button><Link href="/learn" className="inline-flex items-center justify-center gap-2 rounded-2xl bg-white/15 px-5 py-3.5 font-black text-white hover:bg-white/20">Explore topics <ArrowRight size={18}/></Link></div></section></div></main>;

 if(!q)return <main className="min-h-screen bg-violet-50 p-6"><div className="mx-auto max-w-xl rounded-[2rem] bg-white p-10 text-center shadow-xl"><Target className="mx-auto text-violet-600" size={42}/><h1 className="mt-4 text-2xl font-black text-[#071b3a]">Mission unavailable</h1><p className="mt-2 text-slate-500">{error||"Please try again later."}</p><button onClick={restart} className="mt-6 rounded-2xl bg-violet-600 px-5 py-3 font-black text-white">Try again</button></div></main>;

 const difficulty=q.difficulty==='easy'?'Starter':q.difficulty==='hard'?'Challenge':'Core';
 return <main className="min-h-screen bg-gradient-to-br from-violet-50 via-white to-cyan-50"><header className="border-b border-white bg-white/85 backdrop-blur"><div className="mx-auto flex max-w-5xl items-center justify-between px-5 py-4"><Link href="/dashboard" className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Dashboard</Link><div className="flex items-center gap-2"><span className="rounded-full bg-violet-50 px-3 py-2 text-sm font-black text-violet-600"><Trophy size={15} className="mr-1 inline"/> Level {level}</span><span className="rounded-full bg-amber-50 px-3 py-2 text-sm font-black text-orange-600"><Flame size={17} className="mr-1 inline"/> {streak} day streak</span></div></div></header><div className="mx-auto max-w-4xl px-5 py-7 sm:py-10"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.18em] text-violet-600">Question {current+1} of {questions.length}</p><h1 className="mt-1 text-3xl font-black text-[#071b3a] sm:text-4xl">Let’s solve it! 🚀</h1></div><div className="text-right"><p className="text-xs font-bold text-slate-400">Mission XP</p><p className="text-xl font-black text-violet-600">{score}</p></div></div><div className="mt-5 h-3 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-gradient-to-r from-violet-500 via-fuchsia-500 to-cyan-400 transition-all duration-300" style={{width:`${progress}%`}}/></div><section className="mt-7 rounded-[2rem] border border-white bg-white p-6 shadow-xl shadow-violet-100 sm:p-9"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex gap-2"><span className="rounded-full bg-violet-50 px-3 py-1.5 text-xs font-black text-violet-600">{q.topic}</span><span className="rounded-full bg-cyan-50 px-3 py-1.5 text-xs font-black text-cyan-700">{difficulty}</span></div><span className="inline-flex items-center gap-1 rounded-full bg-yellow-50 px-3 py-1.5 text-xs font-black text-orange-600"><Sparkles size={14}/> +{q.points} XP</span></div><p className="mt-5 text-xs font-bold uppercase tracking-wider text-slate-400">Skill: {q.skill}</p><h2 className="mt-3 text-2xl font-black leading-tight text-[#071b3a] sm:text-3xl">{q.prompt}</h2><div className="mt-7 grid gap-3 sm:grid-cols-2">{q.options.map(option=>{const isSelected=selected===option,isCorrect=option===q.answer;let cls="border-slate-200 bg-white hover:-translate-y-0.5 hover:border-violet-300 hover:bg-violet-50";if(selected&&isCorrect)cls="border-emerald-300 bg-emerald-50";else if(selected&&isSelected)cls="border-rose-300 bg-rose-50";return <button key={option} onClick={()=>choose(option)} disabled={Boolean(selected)||saving} className={`flex items-center justify-between rounded-2xl border-2 p-4 text-left font-bold text-[#071b3a] transition ${cls}`}><span>{option}</span>{selected&&isCorrect?<CheckCircle2 className="text-emerald-500" size={21}/>:selected&&isSelected?<XCircle className="text-rose-500" size={21}/>:<span className="h-5 w-5 rounded-full border-2 border-slate-200"/>}</button>})}</div>{selected&&<div className={`mt-5 rounded-2xl p-5 ${selected===q.answer?"bg-emerald-50":"bg-rose-50"}`}><div className="flex items-start gap-3">{selected===q.answer?<CheckCircle2 className="mt-0.5 shrink-0 text-emerald-600" size={21}/>:<XCircle className="mt-0.5 shrink-0 text-rose-600" size={21}/>}<div><p className={`font-black ${selected===q.answer?"text-emerald-700":"text-rose-700"}`}>{selected===q.answer?"Brilliant! Correct answer.":`Not quite — the answer is ${q.answer}.`}</p><p className="mt-1 text-sm leading-6 text-slate-600">{q.explanation}</p></div></div></div>}{error&&<p className="mt-4 rounded-xl bg-amber-50 p-3 text-sm font-bold text-amber-700">{error}</p>}<div className="mt-7 flex justify-end">{selected&&<button onClick={next} className="inline-flex items-center gap-2 rounded-2xl bg-violet-600 px-5 py-3.5 font-black text-white shadow-lg shadow-violet-200 hover:bg-violet-700">{current===questions.length-1?"Finish mission":"Next question"} <ArrowRight size={18}/></button>}</div></section><div className="mx-auto mt-5 max-w-xl rounded-2xl bg-white/70 p-3 text-center"><div className="flex items-center justify-between text-xs font-bold text-slate-500"><span>Level {level}</span><span>{Math.max(0,totalXp-currentLevelStart)} / 100 XP</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-gradient-to-r from-violet-500 to-yellow-300" style={{width:`${levelProgress}%`}}/></div></div><p className="mt-4 text-center text-sm text-slate-400">Your next questions are selected using your learning history. Mistakes are part of learning. 💡</p></div></main>;
}
