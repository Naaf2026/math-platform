"use client";

import { useEffect, useState } from "react";
import { Clock3, Crown, Gift, Heart, RotateCcw, Sparkles } from "lucide-react";

const DEFAULT_SECONDS=45;const MAX_LIVES=3;
type MissionState={combo:number;lives:number;perfect:boolean;timedOut:boolean;difficulty?:string;hintUsed?:boolean};
export default function GameMechanicsSystem(){
 const [seconds,setSeconds]=useState(DEFAULT_SECONDS),[state,setState]=useState<MissionState>({combo:0,lives:MAX_LIVES,perfect:true,timedOut:false}),[boss,setBoss]=useState(false),[retryReady,setRetryReady]=useState(false);
 useEffect(()=>{if(!window.location.pathname.startsWith("/mission"))return;const onState=(e:Event)=>{const d=(e as CustomEvent<MissionState>).detail;if(!d)return;setState(d);setBoss(d.difficulty==="hard");setRetryReady(d.lives>0&&!d.perfect&&!d.timedOut)};const onRetry=()=>{setRetryReady(false);setSeconds(DEFAULT_SECONDS)};const onTimeout=()=>setState(v=>({...v,timedOut:true,perfect:false}));window.addEventListener("fv:mission-state",onState);window.addEventListener("fv:retry-question",onRetry);window.addEventListener("fv:mission-timeout",onTimeout);return()=>{window.removeEventListener("fv:mission-state",onState);window.removeEventListener("fv:retry-question",onRetry);window.removeEventListener("fv:mission-timeout",onTimeout)}},[]);
 useEffect(()=>{if(!window.location.pathname.startsWith("/mission")||state.lives===0||state.timedOut)return;if(seconds<=0){setState(v=>({...v,timedOut:true,perfect:false}));window.dispatchEvent(new CustomEvent("fv:mission-timeout"));return}const id=window.setInterval(()=>setSeconds(v=>Math.max(0,v-1)),1000);return()=>window.clearInterval(id)},[seconds,state.lives,state.timedOut]);
 if(typeof window!=="undefined"&&!window.location.pathname.startsWith("/mission"))return null;const progress=Math.max(0,Math.min(100,seconds/DEFAULT_SECONDS*100));
 return <><div className="fixed left-1/2 top-20 z-[70] flex max-w-[calc(100vw-1rem)] -translate-x-1/2 flex-wrap items-center justify-center gap-2 px-2 sm:top-24"><div className={`flex items-center gap-2 rounded-2xl border bg-white/95 px-3 py-2 text-xs font-black shadow-lg backdrop-blur ${seconds<=10?"border-rose-300 text-rose-600":"border-slate-200 text-slate-700"}`}><Clock3 size={15}/><span>{state.timedOut?"Time bonus lost":`${seconds}s`}</span></div><div className="flex items-center gap-2 rounded-2xl border border-orange-200 bg-white/95 px-3 py-2 text-xs font-black text-orange-700 shadow-lg backdrop-blur"><span>🔥 Combo {state.combo}</span>{state.combo>1&&<b className="rounded-full bg-orange-100 px-1.5 py-0.5">×{Math.min(3,1+Math.floor(state.combo/2)*0.5).toFixed(1)}</b>}</div><div className="flex items-center gap-1 rounded-2xl border border-rose-200 bg-white/95 px-3 py-2 text-xs font-black text-rose-600 shadow-lg backdrop-blur" aria-label={`${state.lives} lives remaining`}>{Array.from({length:MAX_LIVES}).map((_,i)=><Heart key={i} size={15} fill={i<state.lives?"currentColor":"none"} className={i<state.lives?"":"text-slate-300"}/>)}</div>{boss&&<div className="flex items-center gap-1.5 rounded-2xl border border-yellow-300 bg-yellow-50/95 px-3 py-2 text-xs font-black text-yellow-800 shadow-lg"><Crown size={15}/> BOSS</div>}{state.hintUsed&&<div className="flex items-center gap-1.5 rounded-2xl border border-violet-200 bg-violet-50/95 px-3 py-2 text-xs font-black text-violet-700 shadow-lg">💡 Hint used</div>}</div><div className="fixed bottom-20 left-1/2 z-[65] w-[min(560px,calc(100vw-2rem))] -translate-x-1/2 pointer-events-none"><div className="h-1.5 overflow-hidden rounded-full bg-slate-200/70 shadow"><div className={`h-full transition-all duration-1000 ${seconds<=10?"bg-rose-400":"bg-orange-400"}`} style={{width:`${progress}%`}}/></div></div>{retryReady&&state.lives>0&&<div className="fixed bottom-5 left-1/2 z-[75] flex -translate-x-1/2 items-center gap-2 rounded-2xl border border-rose-200 bg-white/95 px-4 py-3 shadow-2xl backdrop-blur"><RotateCcw size={16} className="text-rose-500"/><span className="text-xs font-black text-slate-700">Retry for a fresh chance</span><button onClick={()=>window.dispatchEvent(new CustomEvent("fv:retry-question"))} className="rounded-xl bg-rose-500 px-3 py-2 text-xs font-black text-white">Retry</button></div>}{state.lives===0&&<div className="fixed inset-0 z-[90] flex items-center justify-center bg-slate-950/65 p-5 backdrop-blur-sm"><div className="w-full max-w-md rounded-[2rem] bg-white p-7 text-center shadow-2xl ring-1 ring-rose-200 sm:p-9"><div className="mx-auto flex h-20 w-20 items-center justify-center rounded-3xl bg-rose-50 text-rose-500"><Heart size={40} fill="currentColor"/></div><p className="mt-5 text-xs font-black uppercase tracking-[.2em] text-rose-500">Adventure paused</p><h2 className="mt-2 text-3xl font-black text-[#15233f]">Out of lives</h2><p className="mt-3 text-sm font-bold leading-6 text-slate-500">You used all 3 lives. Start a fresh adventure to try the challenge again.</p><button onClick={()=>window.location.reload()} className="mt-6 inline-flex items-center justify-center gap-2 rounded-2xl bg-gradient-to-r from-orange-500 to-pink-500 px-7 py-4 font-black text-white shadow-lg shadow-orange-200"><RotateCcw size={18}/> Start Fresh Adventure</button></div></div>}{state.perfect&&state.combo>0&&!state.timedOut&&<div className="fixed inset-x-0 bottom-6 z-[76] mx-auto flex max-w-md items-center gap-3 rounded-3xl border-2 border-yellow-300 bg-gradient-to-r from-yellow-50 to-orange-50 p-4 shadow-2xl"><div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl bg-yellow-300"><Sparkles size={21}/></div><div><p className="font-black text-[#15233f]">PERFECT RUN! 🌟</p><p className="text-xs font-bold text-slate-600">No incorrect answers or hints yet.</p></div><Gift size={22} className="ml-auto text-orange-500"/></div>}</>;
}
