# Deployment Trigger

This file is intentionally minimal and is used to verify that the connected Cloudflare deployment automatically follows the GitHub `main` branch.

Latest application source remains on `main`; this marker contains no runtime code.
